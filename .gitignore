node_modules/
.env
.env.local
*.log
dist/
data/

# Foundry build output. src/chain/abi.ts is generated from it and committed,
# so the artifacts themselves add churn to every diff without adding
# information -- regenerate with `forge build && npm run abi`.
out/
cache/

# Generated video work files; the raw clips and the final cut are kept.
brand/video/work/
