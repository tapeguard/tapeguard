{
  "name": "tapeguard",
  "version": "0.1.0",
  "private": true,
  "description": "Correctness layer for tokenised-equity price oracles: halt, corporate-action and earnings guards",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "test:session": "node --test test/session.test.ts",
    "test:guards": "node --test test/guards.test.ts",
    "demo": "node scripts/demo.ts",
    "dev": "node src/server/http.ts",
    "start": "node src/server/http.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "viem": "^2.56.8"
  }
}
